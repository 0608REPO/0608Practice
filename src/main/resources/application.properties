
# Port
server.port=8081

# Context-Path
server.servlet.context-path=/

# Driver Settings
spring.datasource.driver-class-name=oracle.jdbc.driver.OracleDriver
spring.datasource.url=jdbc:oracle:thin:@localhost:1521:xe
spring.datasource.username=scott
spring.datasource.password=tiger

# JSP Settings
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# Mybatis
# Mapper Location
mybatis.mapper-locations=classpath:/mapper/*Mapper.xml

# Static-Path
#spring.mvc.static-path-pattern=/resources/test/**

# Encoding
server.servlet.encoding.enabled=true
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.force=true

# HiddenHttpMethodFilter
spring.mvc.hiddenmethod.filter.enabled=true

# Multipart
spring.servlet.multipart.enabled:true
spring.servlet.multipart.location:C:\\multi\\00.spring
#spring.servlet.multipart.file-size-threshold: (default: 0B)
#spring.servlet.multipart.max-file-size: (default: 1MB)
#spring.servlet.multipart.max-request-size: (default: 10MB)

# Props : Private Properties
props.name=\uc548\ub155\ud558\uc138\uc694

spring.transaction.rollback-on-commit-failure=true

